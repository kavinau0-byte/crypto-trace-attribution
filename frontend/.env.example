# Base URL of the SIH26182 FastAPI backend.
# Copy this file to `.env` and edit if you run the backend on a different
# host or port. If unset, the app falls back to http://localhost:8000.
VITE_API_BASE_URL=http://localhost:8000
